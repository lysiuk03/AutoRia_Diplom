pipeline {
    agent any
    stages {
        stage('Start Grafana') {
            steps {
                script {
                    sh "docker-compose -f docker-compose.grafana.yaml up -d"
                }
            }
        }
    }
}